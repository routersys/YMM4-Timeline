<UserControl x:Class="Timeline.TimelineToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Timeline"
             xmlns:views="clr-namespace:YukkuriMovieMaker.Views;assembly=YukkuriMovieMaker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:TimelineToolViewModel}">
    <Grid>
        <views:TimelineView DataContext="{Binding TimelineViewModel}" />
    </Grid>
</UserControl>
